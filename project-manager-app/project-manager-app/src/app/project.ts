export interface IProject {
    projectId: number,
    endDate: string,
    priority: number,
    startDate: string,
    title: string,
    projectManagerId: number,
    taskCount: number,
    taskCompletedCount: number
}